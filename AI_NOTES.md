# AI Usage Notes

## AI-generated Parts
I used ChatGPT to generate the initial FastAPI project structure and REST API endpoints.

## Validation and Changes
I reviewed the generated code, tested every endpoint using Swagger UI, and verified that Add, View, Filter, Total, and Delete operations worked correctly.

## AI Suggestions Not Used
I chose JSON file storage instead of using a database because the assignment allowed local file storage and it kept the project simple.